# Shadow smoke v17.2

The first attempted replay used `tests/fixtures/paired/multi01_scanner_packet.json`
and was stopped fail-closed with `packet_incomplete` before Hermes invocation.
That file is a market-universe fixture, not a decision packet.

The canonical decision-packet fixture for this smoke is
`tests/fixtures/shadow_smoke_packet_v17.2.json`. The run remains offline and
delivery-disabled; no gateway call or order is permitted. The evidence file is
generated by the runner as `run.json` and must record `orders_sent: 0`.
