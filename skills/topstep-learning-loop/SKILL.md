---
name: topstep-learning-loop
description: Coordinate evidence-triggered Topstep debrief, hourly review, five-hour planning, and daily learning.
---

# Learning Loop

1. Debrief each new canonical learning-eligible completed outcome exactly once.
2. Run hourly supervision only when new episodes exist.
3. Replace the current five-hour plan only when new hourly reviews exist.
4. Run the daily journal only after the trading session and only when attributable episodes exist.
5. Retrieve durable memory once per loop. Only the daily loop may write memory, and only from repeated attributable evidence.
6. Cognitive candidates remain proposed unless the deterministic worker confirms the operator-enabled activation policy and evidence threshold.
7. Every model call is isolated and receives bounded evidence. No learning loop may submit an intent, access the gateway, or modify execution policy.

## Learn like a trader

1. Debrief every attributable completed trade from its immutable pre-decision packet: why it entered, why it exited, geometry, quantity, management, favorable/adverse excursion, rollback, what worked, what failed, and plausible alternatives.
2. Keep one trade episodic. Promote a lesson only from repeated comparable outcomes and preserve contradictions.
3. Evaluate `NOTHING`, `HOLD`, direction, quantity, geometry, and exits. For flat `NOTHING`, preserve a structured counterfactual: the developing movement, the condition or price that would have offered favorable participation, invalidation, and the later observed path.
4. Classify each matured flat `NOTHING` as `justified_abstention`, `avoided_adverse_movement`, `missed_directional_participation`, or `ambiguous` by comparing the declared forecast and participation condition with the observed path. Label the actual outcome as no trade and never invent counterfactual fills, geometry, PnL, reward, or punishment. Use the classification to improve timing questions and accountability, never as a deterministic entry rule, quantity pressure, durable memory, trade quota, or fear-driven cooldown.
5. For `missed_directional_participation`, name the setup taxonomy when evidence supports it: continuation at range edge, pullback reclaim, breakout acceptance, failed-breakout fade, or transition (5m/60m conflict resolving). Preserve the declared participation condition, invalidation, and observed path; do not collapse missed participation into generic regret language.
6. Treat each prior `change_condition` as accountable. If later evidence meets it, record whether Hermes changed action or merely moved the threshold and repeated the same thesis (`met`, `reassessed`, `threshold_moved`, or `not_applicable`). Worker-supplied `change_condition_review` values (`unmet`, `met_with_reassessment`, `met_without_reassessment`, `unknown`) are advisory anchors only.
7. Hourly and planning loops may improve attention and questions from decision episodes, but they may not create entry pressure, anti-abstention pressure, or quantity pressure.

## Cognitive self-improvement

Hermes may propose one compact, versioned `core_prompt` clause replacement with `operation=replace`, one exact `expected_old_text`, minimal `replacement_text`, supporting episode IDs, expected effect, evaluation metric, and rollback condition. Only one overlay is active at a time. A proposal has no trading influence until a later independent review activates it from comparable evidence after contradiction review.
