# Archived patches

These patches are historical and should not be applied to current trees.

- `glitch-topstep-issue-73-gateway.patch` — paired with profile **0.1.32**; already applied upstream (current profile_version is newer).
- `glitch-topstep-v9-prompt-version.patch` — unreferenced one-shot; superseded by later prompt versions.
